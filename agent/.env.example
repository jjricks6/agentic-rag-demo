# Recall - Local Development Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# These values come from Terraform outputs after deployment:
#   cd terraform/environments/dev
#   terraform output -json agent_config

# Required — AWS resource names from Terraform
DOCUMENTS_BUCKET=agentic-rag-demo-documents-dev-YOUR_ACCOUNT_ID
VECTORS_BUCKET=agentic-rag-demo-vectors-dev-YOUR_ACCOUNT_ID
VECTOR_INDEX=agentic-rag-demo-index-dev

# Optional — defaults shown
# AWS_REGION=us-east-1
# ENVIRONMENT=dev
# EMBEDDING_MODEL=amazon.titan-embed-text-v2:0
# LLM_MODEL=us.anthropic.claude-3-5-sonnet-20241022-v2:0
# VECTOR_DIMENSIONS=1024

# Agent mode: "local" runs in-process, "agentcore" calls AgentCore Runtime
# AGENT_MODE=local
# AGENTCORE_RUNTIME_ARN=arn:aws:bedrock-agentcore:us-east-1:YOUR_ACCOUNT_ID:runtime/YOUR_RUNTIME_ID

# SSM Parameter Store — set this to skip .env and load from SSM instead
# SSM_PARAMETER_PREFIX=/agentic-rag-demo/dev
